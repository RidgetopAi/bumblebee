```typescript
interface User {
  name: string;
  age: number;
}

const user: User = { name: "Alice", age: 30 };
console.log(user);
```

Here is some regular text between code blocks.

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("World"))
```

And another paragraph after.
