# Test Language Badge

Here's a TypeScript code block:

```typescript
function hello() {
  return "world";
}
```

Here's a JavaScript code block:

```javascript
const test = () => {
  console.log("Testing badges!");
};
```

Here's a code block with no language:

```
plain text code
no language specified
```
